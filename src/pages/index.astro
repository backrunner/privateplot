---
import { ArticleList } from '../components/business/ArticleList';
import Page from '../components/layout/Page.astro';
import { getPaginatedArticles } from '../mock/articles';

const { articles, hasMore, total } = await getPaginatedArticles(1);
---

<Page>
	<ArticleList client:load initialArticles={articles} initialHasMore={hasMore} total={total} />
</Page>
